# Customised-Virtual-File-System-CVFS-Project
A C-based simulation of a Unix-like Virtual File System that implements core OS file management concepts such as inode structure, file tables, permissions, and system calls. The project supports commands like creat, read, write, ls, unlink, and man, providing an interactive shell to manage files entirely in memory.
